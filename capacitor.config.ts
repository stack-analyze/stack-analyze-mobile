import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stackanlyze.app',
  appName: 'stack-anlyze',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
